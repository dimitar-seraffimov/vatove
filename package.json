{
  "name": "vatove",
  "version": "0.1.0",
  "private": true,
  "description": "Intervals-first geospatial biometric PWA",
  "engines": {
    "node": ">=24",
    "npm": ">=11"
  },
  "workspaces": [
    "apps/*",
    "services/api",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @vatove/contracts && npm run build --workspace @vatove/api && npm run build --workspace @vatove/web",
    "typecheck": "npm run build --workspace @vatove/contracts && npm run typecheck --workspace @vatove/api && npm run typecheck --workspace @vatove/web",
    "test": "npm run build --workspace @vatove/contracts && npm run test --workspace @vatove/contracts && npm run test --workspace @vatove/api && npm run test --workspace @vatove/web",
    "lint": "npm run lint --workspace @vatove/web",
    "dev:web": "npm run dev --workspace @vatove/web",
    "dev:api": "npm run dev --workspace @vatove/api"
  }
}
